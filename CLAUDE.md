# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The authoritative project guidance is in `AGENTS.md` (shared with Cursor / Codex / Aider via the cross-tool convention). It's imported below so Claude Code picks up the same content without an extra round-trip:

@AGENTS.md

Cross-session feedback from the user is at `~/.claude/projects/C--Projetos-Genexus18MCP/memory/`. Read `MEMORY.md` there before acting on anything that touches established patterns.
